# Tem um bug no pdf-reader padrão
gem uninstall pdf-reader
gem install pdf-reader -v 1.2.0

# cermine
java -cp cermine.jar pl.edu.icm.cermine.PdfNLMContentExtractor -path paper3.pdf

#parscit
cpan App::cpanminus # prepare to install Perl modules
cpanm Getopt::Long --sudo

	- Class::Struct
	- Getopt::Long
	- Getopt::Std
	- File::Basename
	- File::Spec
	- FindBin
	- HTML::Entities
	- IO::File
	- POSIX
	- XML::Parser // apt-get install libxml-parser-perl
	- XML::Twig
	- XML::Writer
	- XML::Writer::String

Install CRF++

// nao funciona
sudo apt-add-repository 'deb http://cl.naist.jp/~eric-n/ubuntu-nlp oneiric all'
sudo apt-get update
sudo apt-get install libcrf++-dev crf++

// tem que ser pelo source
wget ftp://ftp.cn.debian.org/gentoo/distfiles/CRF++-0.58.tar.gz
tar xvzf CRF++-0.57.tar.gz
cd CRF++-0.57
./configure 
make
sudo make install
sudo cp ./.libs/libcrfpp.so.0 /usr/lib // copiar o arquivo para as libs pq nao copiou 

# GERAL pra ParsCit e CiteSeer (https://github.com/academia-edu/biblicit)
sudo apt-get install poppler-utils
sudo apt-get install ghostscript
sudo apt-get install abiword
	
	#Perl Modules
	sudo cpan install Digest::SHA1
	sudo cpan install String::Approx


// Required to use the CiteSeer algorithm


// running parscite
./citeExtract.pl -m extract_all -i raw /var/www/met.dev/_testing/paper-01.pdf